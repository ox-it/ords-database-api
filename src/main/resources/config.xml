<?xml version="1.0" encoding="ISO-8859-1" ?>

<configuration>

	<!-- Allow environment variables to override file-based defaults -->
   <system/>
   <env />
   
  	<!-- This is the database properties file -->
   <properties fileName="database.properties"/>

</configuration>